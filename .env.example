# Required: use a random secret of at least 32 bytes in every deployed environment.
JWT_SECRET=replace-with-a-random-secret-at-least-32-bytes-long

# Per-account daily limits (reset at midnight in Asia/Tokyo).
AI_DAILY_LIMIT=20
AZURE_OCR_DAILY_LIMIT=5
